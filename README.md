# This project contains the full source code for a self navigating ROS2 robot

## This includes various sensor drivers such as a depth camera (used for creating a local costmap based off of a 3D PointCloud), motor encoders, tof sensors and microcontrollers

## Local voice recognition, self navigation functions (including dynamic obstacle avoidance and safety behavior) and computer vision are also implemented

